import { StyleSheet } from 'react-native';

export const editAcountStyle = StyleSheet.create({
    subtitle:{
        fontFamily: "Inter",
        fontSize: 16,
        fontWeight: "bold",
        color: "#3161B2",
        textAlign: "left",
        marginBottom: 15,
        width: '100%',
        borderRadius: 24
    },
    formContainer: {
        marginBottom:50,
        justifyContent: 'center',
    },

});
