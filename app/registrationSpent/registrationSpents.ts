import { StyleSheet } from 'react-native';

export const registrationSpents = StyleSheet.create({
    input: {
        backgroundColor: '#F0F4F8', // Cor de fundo do input
        borderRadius: 12, // Bordas arredondadas
        paddingVertical: 12, // Espaçamento vertical
        paddingHorizontal: 20, // Espaçamento horizontal
        fontSize: 14,
        color: '#4D9BD7', // Cor do texto
        borderWidth: 1,
        borderColor: '#D1D9E6', // Cor da borda
        width: '100%',
    },
    iconContainer: {
        top: 12, // Ajuste a posição da seta
        right: 10, // Ajuste a posição da seta
    },
    selectedValue: {
        marginTop: 10,
        fontSize: 16,
    }
});
export default registrationSpents; 
